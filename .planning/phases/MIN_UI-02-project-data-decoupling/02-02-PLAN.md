---
phase: MIN_UI-02-project-data-decoupling
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [index.html]
autonomous: true
requirements: [02-02]
must_haves:
  truths:
    - "index.html fetches /api/projects dynamically"
    - "Project cards are generated and inserted into the DOM dynamically"
  artifacts:
    - path: "index.html"
      provides: "Dynamic cases rendering logic"
      contains: "fetch"
---

<objective>
Update `index.html` to fetch the list of projects from the backend and dynamically render them.

Purpose: Decouple database structure from static HTML presentation.
Output: index.html updated with dynamic fetching.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/MIN_UI-02-project-data-decoupling/02-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace hardcoded project cards with dynamic container and fetch request</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
    - Identify the grid container in index.html for case files.
    - Replace the hardcoded cards with an empty div `<div id="projects-container" class="grid grid-cols-1 md:grid-cols-[repeat(auto-fit,minmax(380px,1fr))] gap-[30px]"></div>`.
    - Write a script function `loadProjects()` that fetches `http://127.0.0.1:8000/api/projects`.
    - Iterate the response and construct HTML matching the original card design, including the exact dynamic arguments for openModal().
    - Insert the HTML to `#projects-container`.
    - Ensure GSAP hover bindings or animations are bound correctly after elements are inserted.
  </action>
  <verify>grep -q "loadProjects" index.html</verify>
  <acceptance_criteria>
    - index.html contains an element with id "projects-container"
    - index.html script tags contain a fetch request to "/api/projects"
  </acceptance_criteria>
  <done>index.html fetches and renders projects dynamically</done>
</task>
</tasks>

<verification>
- [ ] grep -q "projects-container" index.html
</verification>

<success_criteria>
- All tasks completed
- index.html renders projects fetched from the API
</success_criteria>

<output>
After completion, create `.planning/phases/MIN_UI-02-project-data-decoupling/02-02-SUMMARY.md`
</output>
