---
phase: MIN_UI-03-secure-channel-form-handling
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [index.html]
autonomous: true
requirements: [03-02]
must_haves:
  truths:
    - "index.html intercepts contact form submit and triggers POST /api/transmit"
    - "Dossier terminal outputs secure status alerts"
  artifacts:
    - path: "index.html"
      provides: "Contact form dynamic wiring and event loop"
      contains: "contact-form"
---

<objective>
Update `index.html` to submit the secure contact form via AJAX fetch request to the `/api/transmit` endpoint.

Purpose: Prevent standard page refresh and display terminal status feedback.
Output: index.html updated with contact form handlers.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/MIN_UI-03-secure-channel-form-handling/03-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add IDs and intercept handler to contact form in index.html</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
    - Add id="contact-form" to the contact form element.
    - Add id="contact-name", id="contact-email", and id="contact-message" to the respective input/textarea elements.
    - Change button type to type="submit".
    - Write a script in index.html that listens for the 'submit' event on #contact-form.
    - Intercept submission using event.preventDefault(), read values, and post them to `http://127.0.0.1:8000/api/transmit`.
    - Render terminal-style loading, success, or error status messages inside `div#transmission-log`.
  </action>
  <verify>grep -q "contact-form" index.html</verify>
  <acceptance_criteria>
    - index.html contains form element with id="contact-form"
    - index.html script block contains fetch request to "/api/transmit"
  </acceptance_criteria>
  <done>index.html intercept AJAX contact form wired</done>
</task>
</tasks>

<verification>
- [ ] grep -q "contact-form" index.html
</verification>

<success_criteria>
- All tasks completed
- Contact form submits AJAX payload correctly
</success_criteria>

<output>
After completion, create `.planning/phases/MIN_UI-03-secure-channel-form-handling/03-02-SUMMARY.md`
</output>
