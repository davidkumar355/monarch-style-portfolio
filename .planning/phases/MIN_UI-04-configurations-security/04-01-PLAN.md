---
phase: MIN_UI-04-configurations-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, .gitignore, backend/.env.example]
autonomous: true
requirements: [04-01]
must_haves:
  truths:
    - "backend/.env.example template contains SendGrid credential keys"
    - ".gitignore contains .env entry"
    - "index.html uses centralized CONFIG object to wire footer social links"
  artifacts:
    - path: "backend/.env.example"
      provides: "Environment variables setup guide for SendGrid"
      contains: "SENDGRID_API_KEY"
    - path: ".gitignore"
      provides: "Git ignore exclusions for security credentials"
      contains: ".env"
    - path: "index.html"
      provides: "Centralized social links CONFIG block"
      contains: "CONFIG"
---

<objective>
Setup security configurations, ignore credential files, and centralize social media links in the frontend.

Purpose: Prevent credentials leak and simplify link updates.
Output: .gitignore, backend/.env.example, index.html config block.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/MIN_UI-04-configurations-security/04-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create backend/.env.example and root .gitignore</name>
  <files>backend/.env.example, .gitignore</files>
  <action>
    - Create backend/.env.example containing placeholder keys for SENDGRID_API_KEY, TO_EMAIL, and FROM_EMAIL.
    - Create .gitignore containing .env, __pycache__/, *.pyc, and local testing cache files.
  </action>
  <verify>test -f backend/.env.example && grep -q ".env" .gitignore</verify>
  <acceptance_criteria>
    - backend/.env.example exists
    - .gitignore exists and ignores .env
  </acceptance_criteria>
  <done>Env example and gitignore created</done>
</task>

<task type="auto">
  <name>Task 2: Implement centralized CONFIG object in index.html and update footer links</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
    - Define a `const CONFIG` block at the top of the script tag in index.html containing URL links for linkedin, github, and instagram.
    - Update social links in the footer to use dynamic references or update their href values programmatically using the configuration object.
  </action>
  <verify>grep -q "const CONFIG" index.html</verify>
  <acceptance_criteria>
    - index.html defines CONFIG social links
    - footer links use dynamic configuration references
  </acceptance_criteria>
  <done>index.html configuration block wired</done>
</task>
</tasks>

<verification>
- [ ] test -f backend/.env.example && grep -q ".env" .gitignore
- [ ] grep -q "const CONFIG" index.html
</verification>

<success_criteria>
- All tasks completed
- Security and configuration files loaded successfully
</success_criteria>

<output>
After completion, create `.planning/phases/MIN_UI-04-configurations-security/04-01-SUMMARY.md`
</output>
