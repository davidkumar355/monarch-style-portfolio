# Resend API Key obtained from Resend Dashboard (e.g. re_...)
RESEND_API_KEY=re_your_api_key_goes_here

# Recipient Email (where briefings will be routed)
TO_EMAIL=david@yourdomain.com

# Verified Sender Email (use onboarding@resend.dev by default for testing)
FROM_EMAIL=onboarding@resend.dev
